import { NewFilterPipe } from './new-filter.pipe';

describe('NewFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NewFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
